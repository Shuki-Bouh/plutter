import test_import
import test_import2

figure Dessin {
    xlabel "Position x (m)"
    ylabel "Position y (m)"

    x = 5
    y = x
    y=(6, 6)
    z = [(4, 4), (x, y), (6, 4), (5,2)]

    a = (1, (5,5))

    draw a
    draw y
    draw ((1,5), (5,5))
    draw b
}